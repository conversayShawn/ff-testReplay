{
  "name": "testr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "firefox": "npx cypress run --record --key --config video=true --browser firefox --parallel",
    "chrome": "npx cypress run --record --key --config --browser chrome --parallel",
    "webkit": "npx cypress run --record --key --config video=true --browser webkit --parallel"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "playwright-webkit": "^1.35.1"
  },
  "dependencies": {
    "cypress": "^12.17.1"
  }
}
